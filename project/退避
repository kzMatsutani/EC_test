<td>
                                <form class="item-num" action="" method="post">
                                    <input id="item_num" type="text" name="num" value="<?=h($product['num'])?>">
                                    <input type="hidden" name="cart_id" value="<?=h($product['cart_id'])?>">
                                    <input type="submit" name="change_num_product_in_cart" value="変更">
                                </form>
                            </td>
                            <?php $price = $product['price'] * $product['num']; ?>
                            <?php $num_total += $product['num']; ?>
                            <?php $sub_total += $price; ?>
                            <td id="item-price" value="<?=$product['price']?>"><?=number_format($product['price'])?>円</td>
                            <td id="total"><?=number_format($price)?>円</td>






javascript配列内の合計値は
let result = [48, 75, 92, 61, 54, 83, 76];
let total = result.reduce(function(sum, element){
  return sum + element;
}, 0);

console.log(total);
>> 489



<div class="chart-type">
                <p class="blue-graph">
                    チャート1(青)
                    <select name="graph-blue-chart" id="graph-blue-chart">
                        <option value="blue-sales-line">売上(線)</option>
                        <option value="blue-sales-bar">売上(棒)</option>
                        <option value="blue-nums-line">個数(線)</option>
                        <option value="blue-nums-bar">個数(棒)</option>
                    </select>
                </p>
                <p class="red-graph">
                    チャート2(赤)
                    <select name="graph-red-chart" id="graph-red-chart">
                        <option value="red-none">なし</option>
                        <option value="red-sales-line">売上(線)</option>
                        <option value="red-sales-bar">売上(棒)</option>
                        <option value="red-nums-line">個数(線)</option>
                        <option value="red-nums-bar">個数(棒)</option>
                    </select>
                </p>
            </div>


document.getElementById('graph-blue-chart').onchange = function() {
        if (myLineCharts) {
            myLineCharts.destroy();
        }
        showChart();
    }


